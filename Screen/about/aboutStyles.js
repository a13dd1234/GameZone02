import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    padding: 4,
    margin: 4,
    backgroundColor: 'yellow'
  },

  content: {
    fontFamily: 'AkayaTelivigala-Regular',
    fontSize: 20,
    // fontWeight: 'bold',
    // With Some Fonts fontWeight Does NOT Work

  }
})

export default styles
